# Override for local development with hot-reload
services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile.local
    image: discord-bot:local
    environment:
      - NODE_ENV=development
    volumes:
      # Mount local files for hot-reload
      - .:/app
      # Preserve node_modules from container
      - /app/node_modules
